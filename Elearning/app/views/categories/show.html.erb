<ul class="categories" style="list-style-type: none">
  <% temp = 0 %>
  <% @words.each do |word| %>
    <li>
      <div>
      	<%#= gravatar_for category, size: 50 %>
	      <%#= link_to category.name, category %>
        
	      <p><%= temp %>. <%= word.en %> : <%= word.vi %></p>
        <% temp = temp + 1 %>
      </div>
    </li>
  <% end %>
</ul>